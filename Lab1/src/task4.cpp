/* #include <iostream>
using namespace std;

#define SQUARE(x) ((x) * (x))

int main(){
    int num;
    cout << "Enter an integer: ";
    cin >> num;

    cout << "The square of " << num << " is " << SQUARE(num) << endl;
    return 0;
} */